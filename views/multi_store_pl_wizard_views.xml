<odoo>
    <!-- Wizard Form View -->
    <record id="view_multi_store_pl_wizard_form" model="ir.ui.view">
        <field name="name">multi.store.pl.wizard.form</field>
        <field name="model">multi.store.pl.wizard</field>
        <field name="arch" type="xml">
            <form string="Multi-Store P&amp;L Report">
                <group>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                    </group>
                    <group>
                        <field name="cin_number" placeholder="e.g., CIN-U74999DL1995PLC065160"/>
                        <field name="company_ids" widget="many2many_tags"/>
                    </group>
                </group>
                <footer>
                    <button name="action_generate_report" string="Generate Report" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Wizard Action -->
    <record id="view_inherit_pl_report_add_wizard_button" model="ir.ui.view">
        <field name="name">account.report.view.inherit.pl.wizard.button</field>
        <field name="model">account.report</field>
        <field name="inherit_id" ref="account_reports.action_account_report_pl"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="%(action_multi_store_pl_wizard)d" string="Multi-Store P&amp;L Wizard" type="action" class="oe_highlight"/>
            </xpath>
        </field>
    </record>
</odoo>
